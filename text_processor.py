def text_processing(input_text):
    input = input_text.strip().lower()
    """
    for future enhancement we can use map all the attributes and the objects to the external 3D model 
    like Three.js library of js or Unity etc

    """
    return input